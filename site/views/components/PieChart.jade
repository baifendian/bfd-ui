include ../includes/api.jade

h1 饼图

pre
  :highlight(lang='js')
    import PieChart from 'bfd-ui/src/PieChart'
    ReactDom. render(<PieChart name="访问来源" radius={{inner:0.75}} animation={{pie:2500,lineText:500}} tooltip={{enabled:true}} url="/data/pieChart.json" />,document.getElementById('demo'))

#demo.demo

h2 属性

ul.api
  +api({
    name: 'data',
    type: 'object',
    isRequired: true,
    desc: '二维数据格式'
  })
    pre
      :highlight(lang='js')
        {
          code: 200,           
          data:[        
            {value:335, name:'直接访问'},
            {value:310, name:'邮件营销'},
            {value:234, name:'联盟广告'},
            {value:135, name:'视频广告'},
            {value:848, name:'搜索引擎'},
            {value:348, name:'百分点'}
          ]
        }

  +api({
    name: 'name',
    type: 'string',
    isRequired: true,
    desc: 'tooltip标题'
  })

  +api({
    name: 'radius',
    type: 'object',    
    desc: '饼图的内圆半径比例'
  })
    pre
      :highlight(lang='js')
        {
          inner:0.75
        }

  +api({
    name: 'animation',
    type: 'object',    
    desc: '饼图的动画时间参数'
  })
    pre
      :highlight(lang='js')
        {
          pie:2500,
          lineText:500
        }
  +api({
    name: 'tooltip',
    type: 'object',    
    desc: '悬浮提示框配置'
  })
    pre
      :highlight(lang='js')
        {
         //禁用提示框，默认启用
         enabled:true
        }
